# The public site. The application lives at app.amlakre.com and is deliberately
# not indexed (see public/robots.txt in the app build).
#
# ⚠ THE OLD `Disallow: /request-access` IS GONE ON PURPOSE, and it is one line to
# put back if that was wrong. It was a thin form page not worth landing on, and
# hiding it made sense. /consultation is not that page: it argues who Amlak suits
# and — more usefully — who it does not, which is a thing people actually search
# for and a reasonable place to arrive. It is in sitemap.xml for the same reason.
# If it should go back to being invisible, `Disallow: /consultation` here and drop
# its <url> from the sitemap; the two must agree, or the site tells crawlers to
# index a page it also tells them not to read.
User-agent: *
Allow: /

Sitemap: https://amlakre.com/sitemap.xml
