# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/build-demo

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local tooling / agent artifacts (not part of the app)
.playwright-mcp/
.claude/settings.local.json

# defensive: a bare .env is never used here — secrets live in .env.local only
.env

# server-only secrets (read locally, pushed to Supabase; never committed)
.env.secrets.local
.env.secrets
